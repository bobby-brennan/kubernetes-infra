charts:
  hackmd:
    namespace: hackmd
    hooks:
      post_install:
      - kubectl apply -f ./ingress.yaml
      - sleep 30
      - ../../scripts/retain.sh
    values:
      postgresql:
        postgresPassword: $HACKMD_POSTGRES_PASSWORD
